#### options

TanStack Query parameters. See the
[TanStack Query query](https://tanstack.com/query/v5/docs/framework/react/reference/useQuery)
docs for more info.

<div className="theme-surface p-4 rounded-md mt-4">
  Metrom React library does not support all TanStack Query parameters, like
  `queryFn` and `queryKey`, are used internally and cannot be overriden. Check
  out the
  [source](https://github.com/metrom-xyz/monorepo/blob/main/packages/react/src/types.ts#L15)
  to see what parameters are not supported.
</div>
